import type { CharacterObservationsState } from "@/features/characters/types";

export const CHARACTER_OBSERVATIONS_STORAGE_KEY = "ark-str:character-observations:v1";

export const DEFAULT_CHARACTER_OBSERVATIONS_STATE: CharacterObservationsState = {
  locales: {},
};
