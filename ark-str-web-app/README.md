# ark-str-web-app

Independent Next.js application package for the Arknights story reader.

## Commands

```bash
npm run dev
npm run verify
```

## Notes

- runtime is bundled-only
- mutable browser state is stored through feature repo/runtime layers over `localStorage`
- root harness docs and orchestration live one directory above this app
